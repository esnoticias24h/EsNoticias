html, body {
  overflow-x: hidden;
}

* {
  box-sizing: border-box;
}

.container, .wrapper, main, article, .hero, .card-grid, .side-stack {
  max-width: 100%;
  padding-left: 16px !important;
  padding-right: 16px !important;
}
